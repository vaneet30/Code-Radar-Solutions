#include<stdio.h>
int main(){
    int arr[n];
    int n;
    scanf("%d",&n);
    for(int i=0;i<n;i++)
    {
        if(arr[i]==1)
        {
            i++;
            print("Sorted");
        }
        else printf("Not Sorted");
           i
    }
    return 0;
}